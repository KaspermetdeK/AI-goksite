# TODO: Implement Game Enhancements and Admin Panel

## 1. Add Emojis to Game Buttons
- Update game button texts in `index.html` to include relevant emojis for each game.

## 2. Scale Treasure Grid Rewards with Bet
- Modify the `revealTile` function in `app.js` to multiply rewards by (bet / 10) for larger bets.

## 3. Add Admin Panel HTML
- Insert a new "Admin Panel" section in the sidebar of `index.html` with inputs for adding items and a list for managing existing items.

## 4. Implement Admin Panel Functionality
- Add JavaScript in `app.js` to handle adding new items, deleting items, editing prices, and saving changes to localStorage.
